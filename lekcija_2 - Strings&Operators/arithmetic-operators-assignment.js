let broj1 = 12;
let broj2 = 5;

let suma = broj1 + broj2; // Sabere brojeve, zatim spremi u varijablu suma
let razlika = broj1 - broj2; // Oduzme brojeve, zatim spremi u varijablu razlika
let proizvod = broj1 * broj2; // Pomnozi brojeve, zatim spremi u varijablu proizvod
let kolicnik = broj1 / broj2; // Podijeli brojeve, zatim spremi u varijablu kolicnik
let ostatak = broj1 % broj2; // Podijeli brojeve, zatim ostatak spremi u varijablu suma

// Ispis ispod
console.log(suma);
console.log(razlika);
console.log(proizvod);
console.log(kolicnik);
console.log(ostatak);
