let suma = 12 + 5;      // Sabere brojeve, zatim spremi u varijablu suma
let razlika = 12 - 5;   // Oduzme brojeve, zatim spremi u varijablu razlika
let proizvod = 12 * 5;  // Pomnozi brojeve, zatim spremi u varijablu proizvod
let kolicnik = 12 / 5;  // Podijeli brojeve, zatim spremi u varijablu kolicnik
let ostatak = 12 % 5;   // Podijeli brojeve, zatim ostatak spremi u varijablu suma

// Ispis ispod
console.log(suma);
console.log(razlika);
console.log(proizvod);
console.log(kolicnik);
console.log(ostatak);
